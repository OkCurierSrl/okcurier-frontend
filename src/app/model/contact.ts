export interface Contact {
  id: number;
  name: string;
  phoneNumber: string;
  email: string;
  county: string;
  locality: string;
  street: string;
  number: string;
  blockNumber: string;
  floorNumber: string;
  apartmentNumber: string;
  zipCode: string;
}
