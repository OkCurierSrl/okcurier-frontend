{
  "domain": "${AUTH0_DOMAIN}",
  "clientId": "${AUTH0_CLIENT_ID}",
  "authorizationParams": {
    "audience": "${AUTH0_AUDIENCE}"
  },
  "apiUri": "${API_URI}",
  "appUri": "${APP_URI}",
  "errorPath": "/error"

}
